<Window x:Class="PL.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
        xmlns:local="clr-namespace:PL"
        mc:Ignorable="d"
        Title="MainWindow" Height="640" Width="800" WindowStartupLocation="CenterScreen" ResizeMode="NoResize" >
    <!--Icon="/images/drone_image.jpeg"-->
    <Grid Name="MainGrid">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="60"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <TabControl Margin="0,5,-0.4,0.4" Grid.Row="1" >
            <!--#region Drone-->
            <TabItem Header="Drones">
                <Grid Margin="20">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="28*"/>
                        <ColumnDefinition Width="445*"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition  Height="*"/>
                        <RowDefinition  Height="60"/>
                    </Grid.RowDefinitions>
                    <StackPanel Orientation="Horizontal" Grid.Row="1" Margin="20,0,0,0" Grid.ColumnSpan="2" >
                        <Button x:Name="bAdd" MinWidth="120" Background="#FFC5B053" Content="Add new drone" Click="AddDroneButton_Click"/>
                        <Label Margin="20,0,0,0"  Height="25" Content="Drone ID:" />
                        <ComboBox x:Name="cmbDronesID"  Height="25"  Width="100" DisplayMemberPath = "DroneId" SelectedValuePath="DroneId" Margin="40,0,0,0"/>
                    </StackPanel>
                    
                    <!--<ListView x:Name="LVListDrones" ItemsSource="{Binding UpdateSourceTrigger=PropertyChanged}" MouseDoubleClick="LVlistBuses_MouseDoubleClick" Grid.ColumnSpan="2"  >
                        <ListView.View>

                            <GridView>
                                <GridViewColumn Header="Drone Id" DisplayMemberBinding="{Binding Path=droneId, Converter={StaticResource printL},UpdateSourceTrigger=PropertyChanged, Mode=OneWay}"/>
                                <GridViewColumn Header="Model" DisplayMemberBinding="{Binding Converter={StaticResource DateConverter},Path=DateOfBeg, UpdateSourceTrigger=PropertyChanged ,Mode=OneWay}"/>
                                <GridViewColumn Header="Max Weight" Width="80"  DisplayMemberBinding="{Binding Converter={StaticResource ThousandConverter},Path=KmSum, UpdateSourceTrigger=PropertyChanged ,Mode=OneWay}"/>
                                <GridViewColumn Header="Battery" DisplayMemberBinding="{Binding Converter={StaticResource ThousandConverter},Path=KmSum, UpdateSourceTrigger=PropertyChanged ,Mode=OneWay}"/>
                                <GridViewColumn Header="Status">
                                    <GridViewColumn.CellTemplate>
                                        <DataTemplate>
                                            <Image Width="30" Height="30" Source="{Binding Converter={StaticResource StatusConverter},Path=Upstatus,UpdateSourceTrigger=PropertyChanged, Mode=OneWay}"/>
                                        </DataTemplate>
                                    </GridViewColumn.CellTemplate>
                                </GridViewColumn>

                                <GridViewColumn>
                                    <GridViewColumn.CellTemplate>
                                        <DataTemplate>
                                            <ProgressBar x:Name="inprogress"  Background="White" Width="245" Grid.Column="3" HorizontalAlignment="Left" Height="32" Margin="9.715,10.6,0,0" Grid.Row="3"  VerticalAlignment="Top" Minimum="0" Maximum="1200"   Value="{Binding Path=ProgressPrecent, UpdateSourceTrigger=PropertyChanged}" Visibility="{Binding ToolNameVisibility, BindsDirectlyToSource=True}" />
                                        </DataTemplate>
                                    </GridViewColumn.CellTemplate>
                                </GridViewColumn>
                                 --><!--buttons--><!--

                                <GridViewColumn>
                                    <GridViewColumn.CellTemplate>
                                        <DataTemplate>
                                            <StackPanel>
                                                <Button x:Name="bdrive"  Background="Transparent" VerticalAlignment="Center" Click="BusDrive_Click">
                                                    <Image Source="/images/readypx.png" Width="30" Height="30"/>
                                                </Button>

                                            </StackPanel>
                                        </DataTemplate>
                                    </GridViewColumn.CellTemplate>
                                </GridViewColumn>


                                <GridViewColumn>
                                    <GridViewColumn.CellTemplate>
                                        <DataTemplate>
                                            <Button x:Name="btreat" Background="Transparent" VerticalAlignment="Center" Click="BusTreat_Click">
                                                <Image Source="/images/intreatment_24px.png" Width="30" Height="30"/>

                                            </Button>
                                        </DataTemplate>
                                    </GridViewColumn.CellTemplate>
                                </GridViewColumn>
                                <GridViewColumn>
                                    <GridViewColumn.CellTemplate>
                                        <DataTemplate>
                                            <Button x:Name="brefuel" Background="Transparent" VerticalAlignment="Center" Click="BusRefuel_Click">
                                                <Image Source="/images/fuel_24px.png" Width="30" Height="30"/>

                                            </Button>
                                        </DataTemplate>
                                    </GridViewColumn.CellTemplate>
                                </GridViewColumn>
                                <GridViewColumn>
                                    <GridViewColumn.CellTemplate>
                                        <DataTemplate>
                                            <Button  x:Name="bdelete" Background="Transparent" VerticalAlignment="Center" Click="DeletBus_Click">
                                                <Image Source="/images/delete_bin_64px.png" Width="30" Height="30"/>

                                            </Button>
                                        </DataTemplate>
                                    </GridViewColumn.CellTemplate>
                                </GridViewColumn>

                                <GridViewColumn>
                                    <GridViewColumn.CellTemplate>
                                        <DataTemplate>
                                            <Button x:Name="bupdate" Background="Transparent"  VerticalAlignment="Center" Click="UpDateBus_Click">
                                                <Image Source="/images/update_24px.png" Width="30" Height="30"/>
                                            </Button>
                                        </DataTemplate>
                                    </GridViewColumn.CellTemplate>
                                </GridViewColumn>
                                 --><!--finish button--><!--

                            </GridView>
                        </ListView.View>

                        --><!--<ListView.Resources>
                            <Style TargetType="{x:Type ListViewItem}">
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="Salmon"/>
                                    </Trigger>
                                    <Trigger  Property="IsSelected" Value="True">
                                        <Setter Property="Background" Value="LightSalmon" />
                                        <Setter Property="FontWeight" Value="Bold"/>
                                    </Trigger>
                                </Style.Triggers>
                                <Setter Property="Foreground" Value="Black"/>
                            </Style>

                        </ListView.Resources>--><!--
                    </ListView>-->
                </Grid>
            </TabItem>
        </TabControl>
    </Grid>
</Window>